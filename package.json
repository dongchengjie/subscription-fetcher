{
  "name": "subscription-fetcher",
  "type": "module",
  "scripts": {
    "dev": "bun run --env-file=.dev/.env ./index.ts",
    "build": "docker build --pull --rm -t subscription-fetcher .",
    "serve": "docker run -it --rm -e NODE_ENV=dev -p 3000:3000 subscription-fetcher"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "ajv": "^8.18.0",
    "ajv-errors": "^3.0.0",
    "ajv-formats": "^3.0.1",
    "axios": "^1.14.0",
    "fs-extra": "^11.3.4",
    "js-yaml": "^4.1.1",
    "ms": "^2.1.3",
    "nanoid": "^5.1.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/fs-extra": "^11.0.4",
    "@types/js-yaml": "^4.0.9",
    "@types/ms": "^2.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
